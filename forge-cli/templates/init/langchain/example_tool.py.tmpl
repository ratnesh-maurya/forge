"""Example tool for {{.Name}}."""

from langchain_core.tools import tool


@tool
def example_tool(query: str) -> str:
    """An example tool that processes text input."""
    return f"Processed: {query}"
